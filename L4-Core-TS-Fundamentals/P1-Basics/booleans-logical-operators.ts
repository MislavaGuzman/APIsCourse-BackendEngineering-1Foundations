// ==========================================
// Booleans & Logical Operators
// ==========================================









// ==========================================
//  TASKS
// ==========================================

// Task 1: API Route Validation
// You are building middleware to protect a route.
// Determine if a request should be processed. It should be processed ONLY if the payload is valid AND the user has NOT reached their API rate limit.
const isPayloadValid = true;
const hasReachedRateLimit = false;

// TODO: Write your code here to calculate 'shouldProcessRequest' and log the result to the console.




// Task 2: Feature Toggling
// A user is trying to use a premium feature in your application. 
// They can use it if their subscriptionType is strictly equal to "premium" OR if they have an active trial (isTrialActive is true).
const subscriptionType = "basic";
const isTrialActive = true;

// TODO: Write your code here to verify if the user can use the feature, save it in a boolean variable, and log it to the console.